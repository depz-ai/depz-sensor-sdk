@PACKAGE_INIT@

# depz-sensor-sdk-cpp — pure C++17 decode-layer SDK, no external dependencies.
# Exposes the imported target depz::sensor_sdk_cpp (alias depz::sensor_sdk).

include("${CMAKE_CURRENT_LIST_DIR}/depz-sensor-sdk-cpp-targets.cmake")

check_required_components(depz-sensor-sdk-cpp)
